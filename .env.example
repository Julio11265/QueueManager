# Local development can use SQLite by leaving DATABASE_URL empty.
# For Neon, paste the pooled or normal Postgres connection string here.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
